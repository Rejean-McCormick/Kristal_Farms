# Kristal Farms API

Implementation home for the domain/scenario API described by `contracts/api/kristal-farms-api.openapi.yaml` and `docs/api/`. No production service implementation was supplied, so this directory is intentionally scaffold-only.
