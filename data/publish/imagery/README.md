# Published imagery snapshots

This publication channel is separate from the immutable application-data
release under `data/publish/current/`.

`current/imagery_manifest.json` describes the local photographic context
snapshot deployed with the Observatory. The browser consumes a deployment copy
at `apps/web/public/imagery/local-satellite.json`.

Imagery promotion is manual and license-reviewed. It does not change
automatically at runtime.
