# Source packages

These archives are preserved as provenance inputs. Their internal repository names/layouts are not separate canonical repositories. The merged active state lives at the root of `kristal-farms`.
