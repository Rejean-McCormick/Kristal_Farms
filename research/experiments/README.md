# Research experiments

Short-lived, cross-domain experiments belong here when they do not yet justify a production pipeline or a durable domain workstream.

Every experiment should make its inputs, assumptions and outputs explicit. If an experiment becomes operationally important, promote the method into `pipelines/`, contracts and tests rather than letting application code depend on this directory.
